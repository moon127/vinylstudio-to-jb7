# vinylstudio-to-jb7
# Core functionality uses Python stdlib (tkinter, shutil, os, etc.)
# musicbrainzngs is optional — album metadata lookup for id file generation
musicbrainzngs
# To package as a standalone app, optionally install:
# pyinstaller
